const weatherRequest = require('../src/weatherRequest')

/*
test('OpenWeatherMap API returns results', done => {
    
    weatherRequest("Miami")
        .then(data => {
            expect(data.name).toBe('Miami');
            done();
        })
        .catch(err => {
            done(err)
        });

});*/